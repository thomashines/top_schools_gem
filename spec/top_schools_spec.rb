require "spec_helper"

describe TopSchools do
  it "has a version number" do
    expect(TopSchools::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
